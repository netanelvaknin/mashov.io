import styled from 'styled-components';

export const StyledAlert = styled.div`
  position: absolute;
  bottom: 4rem;
  left: 4rem;
  min-width: 25rem;
  min-height: 4rem;
  box-shadow: -.4rem 1.6rem 1.7rem -.6rem rgba(0,0,0,0.17);
  padding: 1rem;
`;